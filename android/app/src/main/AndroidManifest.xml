<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- NFC permissions -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="true" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Network permission for the production build (Release) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="Health Without Borders"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!--
              No NFC intent filters on purpose. HWB reads chips only through
              reader mode, which it holds for as long as the app is in the
              foreground; the OS dispatch system is therefore never involved
              while the app is in use. Registering here would only let a stray
              tap launch HWB with no scan in progress, on an app that holds
              clinical data behind a login. NDEF_DISCOVERED and TECH_DISCOVERED
              never matched anyway: the first had no <data> element and the
              second had no @xml/nfc_tech_filter resource.
            -->
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>